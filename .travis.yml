language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t halldorvil/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true halldorvil/docker-react npm run test